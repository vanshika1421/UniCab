<!DOCTYPE html>
<html>
<head>
  <title>Campus Rides</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body { 
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
      min-height: 100vh;
      background-attachment: fixed;
      position: relative;
    }
    
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="cars" patternUnits="userSpaceOnUse" width="100" height="100"><rect width="100" height="100" fill="none"/><path d="M20 60h60c2 0 4-2 4-4v-20c0-2-2-4-4-4H20c-2 0-4 2-4 4v20c0 2 2 4 4 4z" fill="rgba(255,255,255,0.05)"/><circle cx="30" cy="70" r="8" fill="rgba(255,255,255,0.03)"/><circle cx="70" cy="70" r="8" fill="rgba(255,255,255,0.03)"/></pattern></defs><rect width="100" height="100" fill="url(%23cars)"/></svg>');
      opacity: 0.1;
      z-index: -1;
    }
    
    .container { 
      max-width: 900px; 
      margin: 20px auto; 
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 20px; 
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      padding: 0;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,0.2);
    }
    
    .header {
      background: linear-gradient(135deg, #81c784 0%, #64b5f6 50%, #ffb74d 100%);
      padding: 30px 40px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .header::before {
      content: '🚗';
      position: absolute;
      font-size: 60px;
      opacity: 0.2;
      left: 20px;
      top: 10px;
      animation: float 3s ease-in-out infinite;
    }
    
    .header::after {
      content: '🚙';
      position: absolute;
      font-size: 40px;
      opacity: 0.3;
      right: 30px;
      bottom: 20px;
      animation: float 2s ease-in-out infinite reverse;
    }
    
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }
    
    h1 { 
      color: white;
      font-size: 2.5rem;
      font-weight: 700;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
      margin: 0;
      letter-spacing: 1px;
    }
    
    .nav { 
      background: linear-gradient(135deg, #90caf9 0%, #a5d6a7 100%);
      padding: 20px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: 15px;
    }
    
    .nav-left, .nav-right {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    
    .nav a { 
      text-decoration: none; 
      color: white;
      font-weight: 600;
      padding: 12px 20px;
      border-radius: 25px;
      background: rgba(255,255,255,0.2);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255,255,255,0.3);
      transition: all 0.3s ease;
      font-size: 14px;
    }
    
    .nav a:hover {
      background: rgba(255,255,255,0.3);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }
    
    .nav span { 
      color: white;
      font-weight: 600;
      font-size: 16px;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
    }
    
    .content {
      padding: 40px;
    }
    
    ul { 
      list-style: none; 
      padding: 0;
      display: grid;
      gap: 20px;
    }
    
    li { 
      background: linear-gradient(135deg, #e1f5fe 0%, #f3e5f5 100%);
      margin: 0;
      padding: 25px;
      border-radius: 15px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      color: #424242;
      border: 2px solid #e8eaf6;
      position: relative;
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    li:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    }
    
    li::before {
      content: '🚘';
      position: absolute;
      right: 20px;
      top: 20px;
      font-size: 30px;
      opacity: 0.3;
    }
    
    li b {
      color: #1976d2;
      font-size: 1.1em;
    }
    
    .ride-info {
      margin-bottom: 15px;
      line-height: 1.6;
    }
    
    .driver-info {
      font-size: 0.9rem;
      opacity: 0.9;
      margin-bottom: 15px;
    }
    
    .btn { 
      background: linear-gradient(135deg, #66bb6a 0%, #42a5f5 100%);
      color: white;
      border: none; 
      padding: 12px 25px;
      border-radius: 25px;
      cursor: pointer;
      font-weight: 600;
      font-size: 14px;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(102,187,106,0.3);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0,0,0,0.3);
    }
    
    .btn[disabled] { 
      background: linear-gradient(135deg, #95a5a6 0%, #bdc3c7 100%);
      cursor: not-allowed;
      opacity: 0.7;
    }
    
    .btn[disabled]:hover {
      transform: none;
    }
    
    .no-rides {
      text-align: center;
      padding: 60px 20px;
      background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
      color: #424242;
      border: 2px solid #e8eaf6;
      border-radius: 15px;
      font-size: 1.2rem;
      position: relative;
    }
    
    .no-rides::before {
      content: '🚫🚗';
      display: block;
      font-size: 60px;
      margin-bottom: 20px;
      opacity: 0.7;
    }
    
    @media (max-width: 768px) {
      .container {
        margin: 10px;
        border-radius: 15px;
      }
      
      .header, .nav, .content {
        padding: 20px;
      }
      
      h1 {
        font-size: 2rem;
      }
      
      .nav {
        flex-direction: column;
        text-align: center;
      }
      
      .nav-left, .nav-right {
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🚗 UniCab Campus Rides 🚙</h1>
    </div>
    
    <div class="nav">
      <div class="nav-left">
        <% if (user) { %>
          <span>👋 Welcome, <%= user %>!</span>
        <% } %>
      </div>
      <div class="nav-right">
        <% if (user) { %>
          <% if (role === 'driver') { %>
            <a href="/add">➕ Add Ride</a>
            <a href="/driver/notifications">📋 Ride Bookings</a>
          <% } else { %>
            <a href="/mybookings">📱 My Bookings</a>
          <% } %>
          <a href="/logout">🚪 Logout</a>
        <% } else { %>
          <a href="/login">🔑 Login</a>
          <a href="/register">📝 Register</a>
        <% } %>
      </div>
    </div>
    
    <div class="content">
      <% if (user) { %>
        <form method="GET" action="/" style="margin-bottom:20px; max-width:760px; background:linear-gradient(180deg,#ffffffcc,#f7fbff); padding:14px; border-radius:12px; box-shadow:0 6px 18px rgba(0,0,0,0.06);">
          <div style="display:flex; gap:12px; align-items:center; flex-wrap:wrap;">
            <div style="display:flex; gap:8px; align-items:center;">
              <label style="font-weight:600; margin-right:6px;"><input type="radio" name="mode" value="from" <%= (typeof mode !== 'undefined' && mode === 'from') ? 'checked' : '' %> > Departing from (e.g., Campus → City)</label>
              <label style="font-weight:600; margin-right:6px;"><input type="radio" name="mode" value="to" <%= (typeof mode !== 'undefined' && mode === 'to') ? 'checked' : '' %> > Arriving to (e.g., City → Campus)</label>
            </div>

            <input id="fromInput" name="from" type="text" placeholder="From (city or campus)" value="<%= typeof from !== 'undefined' ? from : '' %>" style="flex:1; padding:10px 12px; border-radius:8px; border:1px solid #ddd; font-size:14px;">
            <input id="toInput" name="to" type="text" placeholder="To (city or campus)" value="<%= typeof to !== 'undefined' ? to : '' %>" style="flex:1; padding:10px 12px; border-radius:8px; border:1px solid #ddd; font-size:14px;">

            <select name="campusLocation" style="padding:10px;border-radius:8px;border:1px solid #ddd;">
              <option value="">— Campus (optional) —</option>
              <option value="Campus" <%= (typeof campusLocation !== 'undefined' && campusLocation === 'Campus') ? 'selected' : '' %>>Campus (generic)</option>
              <option value="Main Campus" <%= (typeof campusLocation !== 'undefined' && campusLocation === 'Main Campus') ? 'selected' : '' %>>Main Campus</option>
              <option value="North Campus" <%= (typeof campusLocation !== 'undefined' && campusLocation === 'North Campus') ? 'selected' : '' %>>North Campus</option>
              <option value="South Campus" <%= (typeof campusLocation !== 'undefined' && campusLocation === 'South Campus') ? 'selected' : '' %>>South Campus</option>
              <option value="Other" <%= (typeof campusLocation !== 'undefined' && campusLocation === 'Other') ? 'selected' : '' %>>Other (type in box)</option>
            </select>
            <input id="otherCampusInput" name="otherCampus" type="text" placeholder="Type campus name" value="" style="display:none; padding:10px;border-radius:8px;border:1px solid #ddd; margin-left:6px;">

            <input name="date" type="date" value="" style="padding:10px;border-radius:8px;border:1px solid #ddd;">

            <input name="seats" type="number" min="1" placeholder="Seats" value="<%= typeof seats !== 'undefined' && seats ? seats : '' %>" style="width:90px; padding:10px;border-radius:8px;border:1px solid #ddd;">

            <button type="submit" class="btn" style="padding:10px 16px;">🔎 Search</button>
            <a href="/" class="btn" style="background:linear-gradient(135deg,#e57373 0%,#ef5350 100%); text-decoration:none; display:inline-block; padding:10px 16px;">Clear</a>
          </div>
        </form>
        <script>
          // Auto-fill visible from/to fields based on mode and campusLocation selection.
          const modeRadios = document.querySelectorAll('input[name="mode"]');
          const campusSelect = document.querySelector('select[name="campusLocation"]');
          const fromInput = document.getElementById('fromInput');
          const toInput = document.getElementById('toInput');
          const otherCampusInput = document.getElementById('otherCampusInput');

          function updateVisibleFields() {
            const mode = Array.from(modeRadios).find(r => r.checked)?.value || '';
            let campus = campusSelect.value || '';

            if (!campus && (mode === 'from' || mode === 'to')) {
              const firstCampusOpt = Array.from(campusSelect.options).find(o => o.value && o.value !== 'Other');
              if (firstCampusOpt) {
                campusSelect.value = firstCampusOpt.value;
                campus = firstCampusOpt.value;
              }
            }

            if (mode === 'from') {
              if (campus && campus !== 'Other') {
                fromInput.value = campus;
                fromInput.readOnly = true;
                fromInput.style.background = '#f3f6ff';
                otherCampusInput.style.display = 'none';
              } else if (campus === 'Other') {
                otherCampusInput.style.display = 'inline-block';
                otherCampusInput.style.width = '220px';
                fromInput.readOnly = false;
                fromInput.style.background = '';
                fromInput.value = otherCampusInput.value || '';
              } else {
                fromInput.readOnly = false;
                fromInput.style.background = '';
                otherCampusInput.style.display = 'none';
              }
              toInput.readOnly = false;
              if (!toInput.value) toInput.placeholder = 'To (city or campus)';
            } else if (mode === 'to') {
              if (campus && campus !== 'Other') {
                toInput.value = campus;
                toInput.readOnly = true;
                toInput.style.background = '#f3f6ff';
                otherCampusInput.style.display = 'none';
              } else if (campus === 'Other') {
                otherCampusInput.style.display = 'inline-block';
                otherCampusInput.style.width = '220px';
                toInput.readOnly = false;
                toInput.style.background = '';
                toInput.value = otherCampusInput.value || '';
              } else {
                toInput.readOnly = false;
                toInput.style.background = '';
                otherCampusInput.style.display = 'none';
              }
              fromInput.readOnly = false;
              if (!fromInput.value) fromInput.placeholder = 'From (city or campus)';
            } else {
              fromInput.readOnly = false; toInput.readOnly = false;
              fromInput.style.background = ''; toInput.style.background = '';
              otherCampusInput.style.display = 'none';
            }
          }

          modeRadios.forEach(r => r.addEventListener('change', updateVisibleFields));
          campusSelect.addEventListener('change', updateVisibleFields);
          otherCampusInput.addEventListener('input', function() {
            const val = this.value || '';
            const mode = Array.from(modeRadios).find(r => r.checked)?.value || '';
            if (mode === 'from') {
              fromInput.value = val;
              fromInput.readOnly = false;
              fromInput.style.background = '';
            } else if (mode === 'to') {
              toInput.value = val;
              toInput.readOnly = false;
              toInput.style.background = '';
            }
          });

          updateVisibleFields();
        </script>

        <% if (!searched) { %>
          <div style="text-align:center; padding:40px; border-radius:12px; background:linear-gradient(180deg,#fff,#fbfdff); box-shadow:0 6px 20px rgba(0,0,0,0.04);">
            <h2 style="margin-bottom:10px; color:#1e3a8a;">Find your ride</h2>
            <p style="color:#475569; margin-bottom:12px;">Enter where you're leaving from or going to, pick a campus (optional), choose a date and seats — then tap Search to see matching rides.</p>
            <small style="color:#94a3b8">No results shown until you perform a search.</small>
          </div>
        <% } else { %>
          <% if (rides.length === 0) { %>
            <div class="no-rides">
              No rides match your search.<br>
              <small>Try broadening the location or reducing the minimum seats.</small>
            </div>
          <% } else { %>
            <ul>
              <% rides.forEach(function(ride, idx) { %>
                <li>
                  <div class="ride-info">
                    <div style="font-size: 1.2em; margin-bottom: 8px;">
                      <b><%= ride.from %></b> ➡️ <b><%= ride.to %></b>
                    </div>
                    <div>
                      🕐 <b><%= ride.time %></b> | 💺 <b><%= ride.seats %></b> seats available | 💰 <b>₹<%= ride.price %></b> per seat
                    </div>
                  </div>
                  <div class="driver-info">
                    👨‍💼 Driver: <b><%= ride.driverName %></b> | 📞 <%= ride.contact %>
                  </div>
                  <% if (user) { %>
                    <form method="POST" action="/book/<%= ride._id %>" style="display:inline">
                      <button class="btn" <%= ride.seats < 1 ? 'disabled' : '' %>>
                        <%= ride.seats < 1 ? '🚫 Full' : '🎫 Book Now' %>
                      </button>
                    </form>
                  <% } %>
                </li>
              <% }) %>
            </ul>
          <% } %>
        <% } %>
      <% } else { %>
        <div style="max-width:720px; margin:30px auto; display:flex; justify-content:center;">
          <div style="width:100%; text-align:center; padding:30px; border-radius:12px; background:#ffffff; box-shadow:0 8px 30px rgba(0,0,0,0.06); border:1px solid #eef2ff;">
            <h2 style="color:#1e3a8a; margin-bottom:12px;">Welcome to UniCab</h2>
            <p style="color:#475569; margin-bottom:16px;">Please log in or register to search and book rides.</p>
            <div style="display:flex; gap:12px; justify-content:center;">
              <a href="/login" class="btn" style="background:linear-gradient(135deg,#4f46e5 0%,#6366f1 100%);">🔑 Login</a>
              <a href="/register" class="btn" style="background:linear-gradient(135deg,#10b981 0%,#34d399 100%);">📝 Register</a>
            </div>
          </div>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>